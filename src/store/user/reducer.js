import {
  initialState,
  ADD_ITEM,
  REMOVE_ITEM,
  ADD_ITEM_COUNT,
  REMOVE_ITEM_COUNT
} from './model';

export const user = (state = initialState,action) => {
  switch(action.type) {
    default:
      return state;
  }
}
